import React, { useState } from 'react';
import { Stack, Button, FormControl, FormLabel, Input, InputGroup, InputRightElement } from '@chakra-ui/react';

const DateForm = () => {
	return (
		<Box>
			<form></form>
		</Box>
	);
};

export default DateForm;
